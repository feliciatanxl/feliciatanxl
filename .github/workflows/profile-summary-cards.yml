name: GitHub-Profile-Summary-Cards

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate-github-profile-summary-cards
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate summary cards
        uses: vn7n24fzkq/github-profile-summary-cards@release
        env:
          GITHUB_TOKEN: ${{ secrets.SUMMARY_GITHUB_TOKEN }}
        with:
          USERNAME: ${{ github.repository_owner }}
          # These two lines stop the action's internal broken git push from killing the workflow
          AUTO_PUSH: false
        continue-on-error: true

      - name: Commit and push generated cards
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs: update profile summary cards [skip ci]"
          file_pattern: "profile-summary-card-output/**"
